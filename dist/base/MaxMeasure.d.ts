/**
 * Calculate the maximum value.
 */
export declare class MaxMeasure {
    max: number;
    constructor();
    /**
     * Add cumultative entry of values, which could be the maximum.
     */
    addEntry(...values: number[]): void;
}
